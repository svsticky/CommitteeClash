{
  "type": "module",
  "scripts": {
    "dev": "node run.mjs dev",
    "prod": "node run.mjs prod",
    "down-dev": "docker compose -f docker-compose.dev.yml down --rmi local",
    "down-prod": "docker compose -f docker-compose.prod.yml down --rmi local"
  }
}
