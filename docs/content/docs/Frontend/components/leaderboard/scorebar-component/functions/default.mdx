---
title: Default
sidebar_position: 1
---

import { CodeBlock } from '@/components/code-block'

[**frontend**](../../../..)

***

[frontend](../../../..) / [components/leaderboard/scorebar-component](..) / default

# Function: default()

<CodeBlock language="ts">
{ `function default(props): Element` }
</CodeBlock>

Defined in: [src/components/leaderboard/scorebar-component.tsx:13](https://github.com/svsticky/CommitteeClash/blob/e5fd1c9fb89fe447a4c38e43ebba51eb1e51230c/frontend/src/components/leaderboard/scorebar-component.tsx#L13)

Scorebar Component
This component renders a horizontal score bar that visually represents a score as a percentage.
The score is clamped between 0 and 100 to ensure it does not exceed the valid range.
The bar's width is animated to transition smoothly when the score changes.

## Parameters

### props

The properties passed to the component.

#### className?
<CodeBlock language="ts">
{`string = ''`}
</CodeBlock>

Optional additional class names for styling the score bar.

#### height?
<CodeBlock language="ts">
{`number = 20`}
</CodeBlock>

The height of the score bar, default is 20 pixels.

#### score
<CodeBlock language="ts">
{`number`}
</CodeBlock>
The score to be represented as a percentage (0-100).

## Returns
<CodeBlock language="ts">
{`Element`}
</CodeBlock>
A JSX element that represents the score bar.
